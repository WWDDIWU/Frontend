import { moduleFor, test } from 'ember-qunit';

moduleFor('view:polymer-outlet', 'Unit | View | polymer outlet');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
