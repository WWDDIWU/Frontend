import { dayProgress } from 'carpe-diem/helpers/day-progress';
import { module, test } from 'qunit';

module('Unit | Helper | day progress');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dayProgress([42]);
  assert.ok(result);
});
