import { dateNameOverview } from 'carpe-diem/helpers/date-name-overview';
import { module, test } from 'qunit';

module('Unit | Helper | date name overview');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateNameOverview([42]);
  assert.ok(result);
});
