import { ifIs } from 'carpe-diem/helpers/if-is';
import { module, test } from 'qunit';

module('Unit | Helper | if is');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifIs([42]);
  assert.ok(result);
});
