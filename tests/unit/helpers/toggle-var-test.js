import { toggleVar } from 'carpe-diem/helpers/toggle-var';
import { module, test } from 'qunit';

module('Unit | Helper | toggle var');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toggleVar([42]);
  assert.ok(result);
});
