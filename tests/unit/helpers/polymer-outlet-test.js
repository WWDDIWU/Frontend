import { polymerOutlet } from 'carpe-diem/helpers/polymer-outlet';
import { module, test } from 'qunit';

module('Unit | Helper | polymer outlet');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = polymerOutlet([42]);
  assert.ok(result);
});
