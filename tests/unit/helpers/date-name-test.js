import { dateName } from 'carpe-diem/helpers/date-name';
import { module, test } from 'qunit';

module('Unit | Helper | date name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateName([42]);
  assert.ok(result);
});
