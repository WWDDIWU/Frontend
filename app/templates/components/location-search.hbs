<div id="sortedLocationsDIV">
  {{#if showSorted}}
    {{#each sortedLocations as |location|}}
      <paper-item class="paper-location" {{action "fillForm" location}}>
        <p>{{location.name}}</p>
      </paper-item>
    {{/each}}
  {{/if}}
</div>
